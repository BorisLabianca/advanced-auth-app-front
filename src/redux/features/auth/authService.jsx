import axios from "axios";

const backendUrl = import.meta.env.ADV_AUTH_BACKEND_URL;
const apiUrl = `${backendUrl}/api/users/`;

const register = async () => {};
const authService = { register };

export default authService;
